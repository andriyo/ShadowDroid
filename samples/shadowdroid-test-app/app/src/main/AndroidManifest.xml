<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Camera is an optional permission-testing surface, not a device requirement. -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".DeepLinkActivity"
            android:exported="true"
            android:label="@string/deep_link_activity_name">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="sample"
                    android:pathPrefix="/deeplink"
                    android:scheme="shadowdroid" />
            </intent-filter>
        </activity>

        <activity
            android:name=".DetailActivity"
            android:exported="false"
            android:label="@string/detail_activity_name" />

        <activity
            android:name=".CoroutinesActivity"
            android:exported="true"
            android:label="@string/coroutines_activity_name" />

        <activity
            android:name=".AltLauncherActivity"
            android:exported="true"
            android:label="@string/alt_launcher_activity_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/main_activity_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- The ShadowDroid net-check probe: the activity must issue this
                 exact URL so the proxy can prove app-specific decryption. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="example.com"
                    android:pathPrefix="/.well-known/shadowdroid-canary/"
                    android:scheme="https" />
            </intent-filter>
        </activity>

        <service
            android:name=".RemoteEchoService"
            android:exported="false"
            android:process=":remote" />
    </application>
</manifest>
